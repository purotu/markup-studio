# Flexbox käytännössä

`flex-direction` määrittää suunnan, `flex-wrap` sallii rivittymisen ja `flex-grow` jakaa ylimääräistä tilaa.

```css
.products {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
}
```