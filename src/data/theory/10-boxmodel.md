# Box model

Jokainen HTML-elementti on laatikko. Sisältöä ympäröivät `padding`, `border` ja `margin`. `width` ja `height` määrittävät laatikon kokoa.

```css
.card {
  padding: 1.5rem;
  border: 1px solid #65722e;
  margin: 1rem;
  box-sizing: border-box;
}
```